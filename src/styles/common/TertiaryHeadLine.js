import styled from "styled-components";
import { defaultFontSize } from "../utility/variables";

const TertiaryHeadLine = styled.h3`
  text-transform: uppercase;
  font-size: ${defaultFontSize};
  font-weight: 700;
`;

export { TertiaryHeadLine };

