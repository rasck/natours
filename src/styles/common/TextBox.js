import styled from "styled-components";

const TextBox = styled.div`
  position: absolute;
  top: 40%;
  left: 50%;
  transform: translate(-50%, -50%);
  /* all inline elements will now be centered */
  text-align: center;
`;

export { TextBox };
