import styled from "styled-components";
import { Center } from "../utility/mixins";

const NavList = styled.ul`
  ${Center};
  list-style: none;
  text-align: center;
  margin-top: 2rem;
  width: 100%;
`;

export { NavList };
